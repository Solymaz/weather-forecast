import React from "react";
import "./Search.css";

const Search = () => {
  return (
    <form>
      <input type="search" className="searchBox" placeholder="Type a city.." />
    </form>
  );
};

export default Search;
